--- charts-original/templates/prometheus/rules-1.14/k8s.rules.container_memory_cache.yaml
+++ charts/templates/prometheus/rules-1.14/k8s.rules.container_memory_cache.yaml
@@ -25,7 +25,7 @@
   - name: k8s.rules.container_memory_cache
     rules:
     - expr: |-
-        container_memory_cache{job="kubelet", metrics_path="/metrics/cadvisor", image!=""}
+        container_memory_cache{job="{{ include "exporter.kubelet.jobName" . }}", metrics_path="/metrics/cadvisor", image!=""}
         * on ({{ range $.Values.defaultRules.additionalAggregationLabels }}{{ . }},{{ end }}cluster, namespace, pod) group_left(node) topk by ({{ range $.Values.defaultRules.additionalAggregationLabels }}{{ . }},{{ end }}cluster, namespace, pod) (1,
           max by ({{ range $.Values.defaultRules.additionalAggregationLabels }}{{ . }},{{ end }}cluster, namespace, pod, node) (kube_pod_info{node!=""})
         )
@@ -39,4 +39,4 @@
           {{- toYaml . | nindent 8 }}
         {{- end }}
       {{- end }}
-{{- end }}
\ No newline at end of file
+{{- end }}
