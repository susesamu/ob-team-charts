--- charts-original/templates/prometheus/rules-1.14/kubernetes-system-kubelet.yaml
+++ charts/templates/prometheus/rules-1.14/kubernetes-system-kubelet.yaml
@@ -265,13 +265,13 @@
         histogram_quantile(0.99,
           sum by ({{ range $.Values.defaultRules.additionalAggregationLabels }}{{ . }},{{ end }}cluster, instance, le) (
             topk by ({{ range $.Values.defaultRules.additionalAggregationLabels }}{{ . }},{{ end }}cluster, instance, le, operation_type) (1,
-              rate(kubelet_pod_worker_duration_seconds_bucket{job="kubelet", metrics_path="/metrics"}[5m])
+              rate(kubelet_pod_worker_duration_seconds_bucket{job="{{ include "exporter.kubelet.jobName" . }}", metrics_path="/metrics"}[5m])
             )
           )
         )
         * on ({{ range $.Values.defaultRules.additionalAggregationLabels }}{{ . }},{{ end }}cluster, instance) group_left(node)
         topk by ({{ range $.Values.defaultRules.additionalAggregationLabels }}{{ . }},{{ end }}cluster, instance, node) (1,
-          kubelet_node_name{job="kubelet", metrics_path="/metrics"}
+          kubelet_node_name{job="{{ include "exporter.kubelet.jobName" . }}", metrics_path="/metrics"}
         )
         > 60
       for: {{ dig "KubeletPodStartUpLatencyHigh" "for" "15m" .Values.customRules }}
@@ -441,7 +441,7 @@
         {{- end }}
       {{- end }}
 {{- end }}
-{{- if .Values.prometheusOperator.kubeletService.enabled }}
+{{- if (include "exporter.kubelet.enabled" .)}}
 {{- if not (.Values.defaultRules.disabled.KubeletDown | default false) }}
     - alert: KubeletDown
       annotations:
@@ -454,11 +454,8 @@
         description: Kubelet has disappeared from Prometheus target discovery.
         runbook_url: {{ .Values.defaultRules.runbookUrl }}/kubernetes/kubeletdown
         summary: Target disappeared from Prometheus target discovery.
-      expr: absent(up{job="kubelet", metrics_path="/metrics"} == 1)
-      for: {{ dig "KubeletDown" "for" "15m" .Values.customRules }}
-      {{- with .Values.defaultRules.keepFiringFor }}
-      keep_firing_for: "{{ . }}"
-      {{- end }}
+      expr: absent(up{job="{{ include "exporter.kubelet.jobName" . }}", metrics_path="/metrics"} == 1)
+      for: 15m
       labels:
         severity: {{ dig "KubeletDown" "severity" "critical" .Values.customRules }}
       {{- if or .Values.defaultRules.additionalRuleLabels .Values.defaultRules.additionalRuleGroupLabels.kubernetesSystem }}
@@ -471,4 +468,4 @@
       {{- end }}
 {{- end }}
 {{- end }}
-{{- end }}
\ No newline at end of file
+{{- end }}
