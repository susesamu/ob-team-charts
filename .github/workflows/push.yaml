name: push - Create PR for new charts

on:
  workflow_dispatch:
    inputs:
      charts:
        description: 'Space-separated list of charts to sync (e.g., "rancher-monitoring/105.0.0-rancher.1 rancher-logging/4.10.0-rancher.21")'
        required: true
        type: string
  push:
    branches:
      - main
    paths:
      - charts


jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/rancher/ci-image/charts
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Install Dependencies
        run: zypper --non-interactive in curl tar gzip git make jq go awk patch

      - name: Checkout current repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0

      - name: Mark git directory as safe
        shell: bash
        run: git config --global --add safe.directory "$(pwd)"

      - uses: rancherlabs/dep-fetch/actions/sync-deps@v0.2.0
        with:
          version: v0.2.0

      - name: Detect new chart version
        id: detect_new_charts
        run: |
          # Use workflow input if manually triggered, otherwise detect from git diff
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            NEW_CHARTS="${{ inputs.charts }}"
            echo "### Charts to Process (Manual Trigger)" >> $GITHUB_STEP_SUMMARY
          else
            # Explicitly fetch the 'before' commit to ensure it's available for diffing
            git fetch origin ${{ github.event.before }}
            NEW_CHARTS=$(git diff --name-only ${{ github.event.before }} ${{ github.sha }} | grep -E '^charts/(rancher-monitoring|rancher-logging)/[^/]*/Chart.yaml$' | sed -e 's|charts/\(.*\)/\(.*\)/Chart.yaml|\1/\2|' | tr '\n' ' ')
            echo "### Charts to Process (Auto-Detected)" >> $GITHUB_STEP_SUMMARY
          fi
          echo "new_charts=${NEW_CHARTS}" >> $GITHUB_OUTPUT
          if [ -n "$NEW_CHARTS" ]; then
            for chart in $NEW_CHARTS; do
              echo "- \`$chart\`" >> $GITHUB_STEP_SUMMARY
            done
          else
            echo "No charts detected." >> $GITHUB_STEP_SUMMARY
          fi

      - name: Read App Credentials from Vault
        if: steps.detect_new_charts.outputs.new_charts != ''
        uses: rancher-eio/read-vault-secrets@0da85151ad1f19ed7986c41587e45aac1ace74b6 # v3
        with:
          secrets: |
            secret/data/github/repo/${{ github.repository }}/github/app-credentials appId | APPID;
            secret/data/github/repo/${{ github.repository }}/github/app-credentials privateKey | PRIVATEKEY

      - name: Generate GitHub App Token
        if: steps.detect_new_charts.outputs.new_charts != ''
        id: generate_token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3
        with:
          app-id: ${{ env.APPID }}
          private-key: ${{ env.PRIVATEKEY }}
          owner: ${{ github.repository_owner }}
          repositories: |
            charts
            org

      - name: Update Downstream Chart and Open PR
        if: steps.detect_new_charts.outputs.new_charts != ''
        env:
          NEW_CHARTS: ${{ steps.detect_new_charts.outputs.new_charts }}
          GH_TOKEN: ${{ steps.generate_token.outputs.token }}
          COMMIT_SHA: ${{ github.sha }}
          SOURCE_REPO: ${{ github.repository }}
          OB_DIR: ${{ github.workspace }}
          CHARTS_DIR: ${{ github.workspace }}/charts-repo
        run: ./.github/workflows/push/run-gha.sh
